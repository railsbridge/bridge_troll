<!DOCTYPE html>
<html>
<head>
  <meta content="text/html; charset=UTF-8" http-equiv="Content-Type"/>
</head>
<body>
Hey <%= @rsvp.user.full_name %>,

<p>Thanks for volunteering for <%= @rsvp.event.title %>!</p>

<% if @rsvp.event.location %>
    <p><%= @rsvp.event.title %> will be held at:
      <%= pretty_print_address(@rsvp.event.location) %>
    </p>
<% end %>

You've signed up for <%= pluralize(@rsvp.rsvp_sessions.count, 'session') %>:
<ul>
  <% @rsvp.rsvp_sessions.each do |s| %>
      <li><%= pretty_print_session(s.event_session) %></li>

  <% end %>
</ul>
<p>
  Visit the <%= link_to 'event page', event_url(@rsvp.event) %> for more info.
</p>

<p>- The Troll</p>
</body>
</html>
