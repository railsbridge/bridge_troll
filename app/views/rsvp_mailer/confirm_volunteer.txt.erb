Hey <%= @rsvp.user.first_name %>,

We're happy to hear you're coming to <%= @event.title %>.

<% if @event.location %>
  <%= @event.title %> will be held at:
  <%= pretty_print_address(@event.location) %>
<% end %>

<% if @rsvp.rsvp_sessions.present? %>
  You've signed up for the following <%= @rsvp.rsvp_sessions.count > 1 ? 'sessions' : 'session'%>
  <% @rsvp.rsvp_sessions.each do |s| %>
  *<%= pretty_print_session(s.event_session) %>
  <% end %>
<% end %>

Visit the <%= link_to 'event page', event_url(@event) %> for more info.

- The Troll
