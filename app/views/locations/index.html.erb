<%= content_for(:header_text, 'Listing locations') %>

<%= render 'shared/locations_table', locations: @locations %>

<%= render 'shared/actions', links: crud_object_nav_links(:location, user_signed_in? ? ['New Location', new_location_path] : nil)
 %>
