<%= @sender.full_name %> has something to say about <%= @event.upcoming? ? 'an upcoming event' : 'a past event' %>:

<blockquote><%= sanitize(@body).gsub(/(\r)?\n/, "<br/>").html_safe %></blockquote>

<%= link_to 'View event details', event_url(@event), :class => 'btn' %>
