= form_for @event do |f|
  .field_contain
    = f.label :name
    = f.text_field :name
  .field_contain
    = f.label :location
    = select "event", "location_id", Location.all.collect { |l| [l.name, l.id]}
  .field_contain
    = f.hidden_field :start_time, :value => Time.now
  .field_contain
    = f.hidden_field :end_time, :value => Time.now.advance({:days => 2})
  .field_contain
    = f.hidden_field :capacity, :value => 25
  .field_contain
    = f.hidden_field :guests_per_user, :value => 1
  = f.submit "Create event"
